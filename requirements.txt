# Core libraries
streamlit==1.30.0
pinecone-client>=2.2.1  # Make sure to use a newer version
pinecone>=2.2.1
langchain==0.1.0
openai==1.13.3  # Keep your original version if it works
python-dotenv==1.0.0

# Document processing libraries
pypdf==3.17.0
docx2txt==0.8
python-docx==0.8.11  # Make sure this is exactly as shown
PyPDF2==3.0.1

# Text processing and embeddings
sentence-transformers==2.2.2
transformers==4.37.2
torch  # Flexible version
nltk==3.8.1

# Other dependencies
pandas==2.1.4